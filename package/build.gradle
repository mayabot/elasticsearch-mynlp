apply plugin: 'kotlin'


dependencies{
    api(project(":plugin:plugin-main"))
}

task esPluginZip(type: Zip,dependsOn:jar) {
    from configurations.runtimeClasspath
    from jar
    from "${project.projectDir}/plugin-metadata"
}
